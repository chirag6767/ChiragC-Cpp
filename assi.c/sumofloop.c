#include <stdio.h>

void main()
{
	int i,n=9,sum=0;
	
	for(i=1;i<=10;i++)
	{		
		//printf("\n%d",i);
		sum = n +  i ;// sum = sum + i; 
		printf("\n%d + %d = %d",n,i,sum);
				
	}
	//printf("\nSum of Loop = %d",sum);
	
}